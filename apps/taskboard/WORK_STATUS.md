# apps/taskboard 진행 상황

> Claude Code 세션이 꺼져도 다음 세션에서 이 파일을 먼저 읽고 이어서 작업합니다.
> "진행 중 작업"이 있으면 그것을 최우선으로 이어가고, 완료되면 `CHANGELOG.md`로 옮긴 뒤 이 파일은 "없음" 상태로 비웁니다.

## 진행 중 작업

없음

<!--
작업이 생기면 아래 형식으로 작성:

### <작업명> (시작: YYYY-MM-DD)
- 목표:
- 완료된 단계:
  -
- 남은 단계 / 다음에 할 일:
  -
- 막힌 점 / 확인 필요:
  -
- 관련 파일:
  -
- 짝 폴더(BT-ADMIN-SERVICE-TASKBOARD) 연관 여부:
-->

## 마지막 업데이트

- 2026-06-12: 초기 템플릿 생성
- 2026-06-12: 공지사항 위젯 공통화(AnnouncementWidget) + 실제 연동, RollingDisplay Redis 라이브 데이터 연동, TaskList Redis 집계 로직 수정, OAuth2 미사용 타입 제거 완료 → CHANGELOG.md 기록
- 2026-06-15: "텍스트만 보이기" 위젯 모자이크(블러+테두리) 버그 수정(widgetVisualStyle 공통 유틸 추출 + TaskCreate/TaskView/TaskList/RollingDisplay 적용), 롤링 전환효과 6종 추가(모자이크/와이프/회전/기본/바운스/랜덤, 총 15종) 완료 → CHANGELOG.md 기록
- 2026-06-15: 전광판 실행 시 값 위젯 하단 색상 줄 제거, Redis 집계 뱃지(MAX/MIN/∑) 노출 제거, 값(value) 정렬(valueAlign) 미적용 수정(TaskView/TaskList/RollingDisplay) 완료. Redis 해시 그룹 집계가 동작 안 하는 것처럼 보이는 원인 조사(코드 결함 아님, 다중 키 테스트 데이터 부재) → CHANGELOG.md 기록
- 2026-06-15: Redis 집계 '평균' 추가 + 집계 방식 UI 노출 조건 완화, Redis 해시키 목록 BE 캐싱(@PostConstruct) + FE 새로고침 버튼, "계산식" 위젯 신설(캔버스 위젯 🔗 드래그 → 변수 바인딩 + 자유 수식 입력) 완료. getRedisValue() 중복 제거하여 redisValue.ts 공통 유틸로 통합 → CHANGELOG.md 기록
- 2026-06-15: 계산식 위젯 변수를 캔버스 미배치 Redis 항목에서 직접 바인딩 가능하도록 개선(`CalcOperand.source`/`aggregation` 추가, `getOperandNumericValue`/`collectRedisHashKeys` 신규, `handleDragEnd`에서 좌측 팔레트 Redis 드래그를 변수 드롭존에서 처리, source 바인딩 시 집계(없음/∑/↑/↓/⌀) 선택 UI 추가, TaskView/TaskList/RollingDisplay의 Redis 폴링 키 수집을 `collectRedisHashKeys`로 통합) 완료 → CHANGELOG.md 기록
- 2026-06-16: 상담그룹 compositeKeys + RTS_READY 합산 완료, HTTP 414 수정(getRedisHashFieldsBatch GET → POST) 완료 → CHANGELOG.md 기록
- 2026-06-16: Redis 키 구조 수정 — IC:GROUP:{compositeKey}:{mediaType} 방식(잘못됨) → IC:GROUP:{mediaType} 해시 키에서 field={compositeKey}로 조회(올바름). TaskView/TaskList/RollingDisplay의 groupHashKeys 및 default 블록 3파일 모두 수정 완료
- 2026-06-18: 디스플레이(선택값 그룹핑)를 레이아웃에서 분리하는 N:M 구조 전환 완료 — TaskDisplayManage.tsx 전면 재작성, TaskList/TaskView/TaskMgmt/RollingDisplay가 displayLayoutId 기준으로 동작 → CHANGELOG.md 기록
- 2026-06-18: TaskMgmt 롤링그룹 편집 UX를 "전광판 우선 선택 → 디스플레이 멀티선택" 구조로 재설계, layoutId별 localStorage 기억 추가 → CHANGELOG.md 기록
- 2026-06-18: 디스플레이/전광판 실행화면 표시값 설정 모달의 큐/상담그룹/상담사 한 줄 정렬이 깨지던 버그 수정(TaskDisplayManage.tsx, TaskView.tsx) — 각 항목을 별도 행으로 분리
- 2026-06-18: 좌측 트리 단일값 Redis 위젯을 REST 5초 폴링에서 WS로 통합 완료(`collectRedisWsSubscriptions`/`mergeWsSubscriptions` 신규, TaskView/RollingDisplay가 단일값 위젯도 큐/그룹/상담사와 같은 WS 소켓으로 구독) → CHANGELOG.md 기록
- 2026-06-18: IC:GROUP:* 단일값 위젯이 디스플레이 선택값(selection.groupIds) 기준으로 그룹을 보여주도록 개선(`buildGroupIdsByHashKey` 신규, 여러 그룹 선택 시 합산) → CHANGELOG.md 기록
- 2026-06-18: 롤링그룹을 "전광판 등록 + 실행 시점 디스플레이 선택" 구조로 재설계 — GroupEditView는 전광판 순서만 선택, `RunOptionsView` 신규(전체 적용/전광판별 개별 선택, groupId별 localStorage 기억) → CHANGELOG.md 기록. apps/taskboard 정식 경로(C:\Users\user\git\BT-ADMIN-FE)가 없는 환경이라 이 F: 사본에서 tsc/eslint로 검증함 — 정식 경로에서 한 번 더 확인 필요
- 2026-06-19: 레이아웃↔디스플레이 1:N 매핑(조인 테이블) 구조를 FE에서 완전 제거, task-view URL을 `:layoutId/:displayId` 직접 조합 방식으로 변경, "디스플레이" 용어를 "뷰 그룹"으로 전면 변경 완료 → CHANGELOG.md 기록. **다음 세션 확인 필요**: ① 실제 브라우저로 task-list/task-mgmt 양쪽 실행 흐름 확인(아직 미실시), ② 기존에 모니터에 띄워둔 task-view URL이 있다면 새 형식(`/task-view/:layoutId/:displayId`)으로 재발급해 교체, ③ BE의 `TaskboardDisplayLayout`/`TB_TK_TASKBOARD_DISPLAY_LAYOUT`(V89)은 이제 FE가 호출하지 않는 미사용 코드로 남아있음 — 정리 여부는 사용자와 별도 논의
- 2026-06-19 세션2: "삐져나오는" UI 버그 전체 점검 — flex row에서 `truncate`만 쓰고 `min-w-0`을 빠뜨려 텍스트가 실제로는 줄어들지 않던 곳 8개 파일에서 수정(TaskBg/TaskMgmt/TaskNotice/TaskDisplayManage/TaskCreate/TaskView/MultiSelectDropdown/RollingDisplay) → CHANGELOG.md 기록. 로그인 정보가 없어 브라우저 스크린샷으로 실측 확인은 못 함(코드 점검 후 eslint/tsc만 통과) — **다음 세션 또는 사용자가 직접 브라우저로 확인 필요**. taskboard dev server는 4208 포트로 띄워둔 상태(이미 누군가 네트워크로 접속해 사용 중인 것으로 보임 — 끄지 않았음)
- 2026-06-19 세션3: TaskDisplayManage 카드/목록 보기 토글 + 선택값 요약 칩(SelectionSummary) 추가, MultiSelectDropdown을 shadcn Checkbox/lucide 아이콘 기반으로 비주얼 개선(색상 아이덴티티는 Tailwind v4 `bg-(--css-var)`로 유지) 완료 → CHANGELOG.md 기록. eslint/tsc만 통과, **브라우저 실측 미실시** — 다음 세션이나 사용자가 직접 확인 필요. (참고: 사용자가 보고한 "task-list 접속 시 웹소켓 무한루프"는 이번 작업과 무관 — 이전 세션에 임시로 띄워둔 중복 taskboard dev-server(4208, IPv6 바인딩)와 정식 serve 프로세스가 같은 포트에서 충돌한 것이 원인이었고, 중복 프로세스를 종료해 해결함)
- 2026-06-19 세션4: TaskMgmt 롤링 그룹 편집에서 같은 전광판을 여러 번 추가할 수 있도록 변경(GroupEditView 클릭=추가/우측 목록에서 제거·순서변경, RunOptionsView "개별 선택" 모드는 layoutId 대신 자리(occurrence) 인덱스로 뷰 그룹을 키 분리) 완료 → CHANGELOG.md 기록. eslint/tsc 통과, **브라우저 실측 미실시** — 사용자가 직접 확인 필요(특히: 같은 전광판 2번 추가 → 실행 옵션에서 자리별로 다른 뷰 그룹 선택 → 롤링 시 실제로 다르게 보이는지)
- 2026-06-19 세션5: 세션4 부작용 수정 — "롤링 순서" 박스가 4개 이상부터 전환효과/간격 영역을 가리던 문제, 순서 박스에 `max-h-52 overflow-y-auto` 적용해 자체 스크롤로 해결 완료 → CHANGELOG.md 기록. eslint/tsc 통과, **브라우저 실측 미실시**
- 2026-06-20: TaskCreate 위젯 스타일에 "값 변경 애니메이션"(6종)·"임계치 색상"(min,color 규칙 여러 개 등록) 추가, TaskView/RollingDisplay 양쪽 실제 실행 화면에도 동일 로직 적용 완료 → CHANGELOG.md 기록. 저장 경로(`widgets: droppedWidgets` 통째 직렬화)와 TaskList 비영향을 코드로 추적 확인. eslint/tsc 통과, **브라우저 실측은 로그인 정보 부재로 미실시** — 사용자가 직접 확인 필요(체크리스트: ①TaskCreate에서 두 옵션 설정 후 저장 ②TaskList에서 위젯 개수 뱃지 정상 표시 ③task-view 실행 시 값 바뀔 때 애니메이션 재생 + 임계치 넘으면 색 바뀌는지 ④TaskMgmt 롤링 실행 중에도 동일하게 보이는지)
- 2026-06-20 세션2: 폰트 크기 단계를 48px→96px로 확장(8px 단위 6단계 추가). 사용자가 "반응형인데 화면 크기에 따라 글자 크기가 안 바뀌는 것 같다"고 의심 제기 → 확인해보니 실제 버그였음: TaskCreate 편집기는 `fontScale=containerWidth/DESIGN_WIDTH`로 캔버스 폭에 비례해 폰트를 그렸지만, TaskView/RollingDisplay 실행 화면은 그 스케일 없이 고정 px로 그리고 있었음. `DESIGN_WIDTH`를 widgetVisualStyle.ts로 공용화하고 신규 훅 `useResponsiveFontScale(imgRatio)`로 실행 화면에도 동일 스케일 적용 완료 → CHANGELOG.md 기록. eslint/tsc 통과, **브라우저 실측 미실시** — 다른 화면 크기에서 같은 레이아웃 띄워 글자 비율 일치하는지 사용자가 직접 확인 필요. 추가로 폰트 패밀리 미리보기 박스가 fontSize를 그대로 따라가 96px일 때 미리보기가 너무 커지던 것을 16px 고정으로 수정
- 2026-06-20 세션3: task-mgmt 5건 일괄 개선 — ①헤더 설명문 제거(TaskMgmt/TaskList/TaskNotice, TaskBg는 원래 없었음) ②롤링 순서 위/아래 버튼 제거하고 @dnd-kit/sortable 드래그 재정렬로 교체(X 제거 버튼은 유지, 상태모델을 number[]→{uid,layoutId}[]로 변경) ③롤링 간격 3~60초→3~180초 ④TaskMgmt/TaskNotice 삭제 confirm을 TaskBg/TaskList와 동일한 모달 팝업으로 교체 ⑤그룹 카드 그리드 lg:grid-cols-3→4(md는 2 유지) 완료 → CHANGELOG.md 기록. eslint/tsc 통과, **브라우저 실측 미실시** — 특히 드래그 재정렬 동작감은 사용자가 직접 확인 필요
- 2026-06-21: fork agent로 TASKBOARD FE+BE 전체 코드 리뷰(버그/일관성/데드코드) 수행 후, 사용자 승인 항목(1,2,3,6,7,8) 적용 완료 — task-mgmt 롤링 재생 table-group 컬럼 누락 버그, useThousandSep 실행화면 미적용 버그, WidgetStyle 데드필드 6개, useGetRedisHashFields 데드코드(FE), BE TaskboardDisplayLayout 전체 수직 슬라이스(엔티티/repo/dto 2개/controller 4엔드포인트/service 메서드들) 삭제 → CHANGELOG.md(FE+BE 양쪽) 기록. FE `npx tsc --noEmit` 통과, BE `./gradlew :BT-ADMIN-SERVICE-TASKBOARD:compileJava` BUILD SUCCESSFUL. **브라우저 실측 미실시**. 남은 것: BFF의 taskboard-display-layout-* 4개/taskboard-redis-hashfields/taskboard-redis-hashfields-batch(저확신) AGG_FLOW 레코드 — 사용자가 직접 DB 조회해서 삭제 여부 결정하기로 함(SQL 쿼리 전달 완료, BE `/redis/hash-fields/**` 컨트롤러 엔드포인트도 같이 정리 후보로 남음)
- 2026-06-22: TaskBg.tsx "AI 생성" 표기를 "자동생성"으로 통일(카드 뱃지/모달 단계 라벨/버튼/자동생성 파일명 접두사), TaskCreate "값 변경 애니메이션 → 하이라이트"의 색상을 고정 노란색에서 사용자 지정(`highlightColor`, CSS 변수로 keyframes에 주입)으로 변경 완료 → CHANGELOG.md 기록. eslint/tsc 통과, **브라우저 실측 미실시**
- 2026-06-22 세션2: TaskCreate 위젯 Ctrl+C/Ctrl+V 복사·붙여넣기 추가 — Clipboard API 대신 localStorage를 클립보드로 써서 같은 출처의 다른 브라우저 창/탭 사이에도 동작하게 함(secure context 제약 회피) 완료 → CHANGELOG.md 기록. eslint/tsc 통과, **브라우저 실측 미실시** — 창 2개 띄워서 실제 복사/붙여넣기 사용자가 직접 확인 필요
- 2026-06-22 세션3: TaskCreate 캔버스 상단의 큐/상담그룹/상담사 미리보기 전용 멀티선택 바를 관련 state/훅/핸들러까지 전부 제거(이 화면은 배치/스타일 설정만 담당하도록 한정), 위젯 선택 시 우측 패널 X/Y/W/H 아래에 "데이터 출처"(`getWidgetDataSourcePath`, 예: Redis 해시키 > IC > CTIQ > SUM_DENY_CNT) 표시 추가 완료 → CHANGELOG.md 기록. eslint/tsc 통과, **브라우저 실측 미실시**
- 2026-06-22 세션4: task-create에서 좌/가운데/우측 내부 스크롤은 정상인데 그 바깥에 전체 페이지 스크롤이 하나 더 생기던 버그 수정 — 원인은 host `<main>`(h-full+overflow-y-auto)이 이미 제한된 높이인데 TaskCreate 루트가 `h-screen`(100vh 고정)을 써서 main 자체가 스크롤 컨테이너가 돼버린 것. 루트를 `h-full`로 바꿔서 해결(apps/host는 안 건드림) → CHANGELOG.md 기록. eslint/tsc 통과, **브라우저 실측 미실시** — 사용자가 직접 스크롤 동작 확인 필요. 참고: TaskView.tsx/RollingDisplay.tsx도 같은 `h-screen` 패턴을 쓰는데 이번엔 손 안 댐(전체화면 API로 쓰는 화면이라 증상이 다를 수 있어 범위에서 제외, 필요시 추후 확인)
- 2026-06-22 세션5: TaskCreate 캔버스에 확대/축소(−/100%/+) 버튼 추가 — 순수 transform: scale 적용이라 드래그 좌표 계산·폰트 스케일에 영향 없음 확인, 저장 대상 아님(매 진입마다 100%로 리셋) 완료. **후속**: 사용자가 확대 후 좌상단→우상단처럼 다른 모서리로 스크롤 이동이 안 된다고 지적 → flex 중앙정렬+transform:scale 조합의 "시작 방향 overflow는 스크롤 불가" 브라우저 기본동작이 원인이었음. zoom>1일 때 중앙정렬 끄고 origin-top-left로 좌상단 고정 우/하 방향 확장으로 변경해 해결 → CHANGELOG.md 기록(둘 다). eslint/tsc 통과, **브라우저 실측 미실시** — 확대 후 사방 스크롤 닿는지, 드래그·리사이즈 정확한지 사용자가 직접 확인 필요
- 2026-06-22 세션6: TaskCreate 4건 한꺼번에 — ①사용자 지정 시계 포맷(`etc-custom`, yyyy/mm/dd/hh24/mi/ss 토큰, `clockFormat.ts` 신규) ②텍스트/배경 색상 스포이드(브라우저 EyeDropper API, Chrome/Edge 한정) ③값 텍스트 위치 세밀조정(`valueOffsetX/Y` px, 방향패드+직접입력, `transform:translate` 적용 — pulse/shake/bounce 애니메이션과 동시 사용 시 transform 충돌 가능성은 알려진 제약으로 기록) ④사용자가 보고한 "task-create에서 IC:CTIQ까지만 보이고 미디어타입 안 보임" 확인 → 테이블/차트 위젯(`getWidgetDataSourcePath`)이 `item.mediaType`을 안 보여주던 게 원인, 보강 완료. 동시에 WS 구독 3개(CTIQ:0/10/IN_TOT)+빈 응답은 버그 아니고 미디어타입 다른 위젯 3개를 등록했을 때의 정상 동작이라고 설명함 → CHANGELOG.md 기록. eslint/tsc 통과, **브라우저 실측 미실시** — 4가지 전부 사용자가 직접 확인 필요(특히 스포이드는 Chrome/Edge에서만)
- 2026-06-22 세션7: 세션6 후속 사용자 피드백 3건 — ①위젯 잠금(Lock) 기능 신규 추가(`DroppedWidget.locked`, 캔버스 호버 버튼+우측 패널 스위치, 자유모드 드래그/리사이즈/방향키/그룹드래그 차단 + 그리드모드는 RGL `static` 활용) — 복사·붙여넣기 반복 중 의도치 않게 미세하게 밀리는 사고 방지 목적 ②세션6에서 알려진 제약으로 남겨뒀던 "값 위치 오프셋 vs pulse/shake/bounce 애니메이션 충돌"을 실제로 고침 — keyframes를 `--tb-offset-x/y` CSS 변수 기반으로 재작성해 애니메이션 재생 중에도 오프셋 기준점 유지(`getValueOffsetStyle` 신규, 기존 인라인 transform 3곳 교체) ③HD 등 16:9가 아닌 배경 이미지에서 세로 가이드라인이 위로 쏠리던 원인 확인 — `imageRatio`가 실제 이미지 비율을 한 번도 안 읽고 `'16/9'` 하드코딩이었음, `DroppableBoard` 배경 `<img>`에 `onLoad`로 `naturalWidth/Height` 읽어 실제 비율로 갱신하도록 수정 → CHANGELOG.md 기록. eslint/tsc 통과, **브라우저 실측 미실시** — 3가지 모두 사용자가 직접 확인 필요. **후속(세션8에서 정정)**: 사용자가 "가이드라인 쏠림 개선 안 됐다" + "위젯 잠금은 위젯별이 아니라 캔버스 전체 단위였으면" 피드백 → 세션8 항목 참조
- 2026-06-22 세션8: 세션7 정정 2건 — ①가이드라인 쏠림의 실제 원인은 이미지 비율이 아니라 `DroppableBoard`(rounded-xl overflow-hidden) 안에 가이드 오버레이를 자식으로 렌더하던 구조였음 — 라벨의 음수 top/left 오프셋이 그 경계에서 잘려 위쪽/가장자리에서 잘 안 보였던 것. 가이드 오버레이를 `DroppableBoard` 밖(`boardContainerRef` 형제)으로 이동해 클리핑 제거 + 눈금자 바깥 패딩 16px→32px로 확대해 가장자리 라벨 여유공간 확보(이미지 비율 onLoad 갱신 자체는 유지) ②위젯 잠금을 위젯별 Lock에서 캔버스 전체 단위 "전체 잠금" 토글로 교체 — `DroppedWidget.locked` 필드/per-widget UI 전부 제거, 상단 툴바(자유/그리드 토글, 되돌리기/다시실행 옆)에 `canvasLocked` state 기반 버튼 하나로 통합, 자유모드 드래그/리사이즈/방향키 + 그리드모드 RGL static 전부 이 단일 플래그로 게이팅 → CHANGELOG.md 기록. eslint/tsc 통과, **브라우저 실측 미실시** — 가이드라인이 모든 가장자리에서 안 잘리는지, 전체 잠금이 자유/그리드 양쪽에서 실제로 모든 위젯을 막는지 사용자가 직접 확인 필요. **후속(세션9에서 정정)**: 사용자가 "버튼 명칭을 위젯 잠금으로" + "가이드라인 여전히 똑같다, 이미지 위쪽에 기본 공간을 넣어달라" 피드백 → 세션9 항목 참조
- 2026-06-22 세션9: 세션8 후속 정정 2건 — ①"전체 잠금" 버튼 라벨/title을 "위젯 잠금"으로 변경(동작은 동일) ②가이드라인 쏠림이 세션8의 패딩 확대(32px)로도 안 고쳐진 이유 확정 — 그때 좌측 눈금자의 `top`을 그대로 16px(`top-4`)에 둬서, 늘어난 32px 패딩과 안 맞아 눈금자 0~100%가 실제 이미지 0~100%보다 16px 더 길게 측정되는 정합 버그가 같이 있었음. `RULER_SIZE`(16px)/`TOP_GUIDE_GAP`(24px) 상수로 분리해 바깥 패딩은 `RULER_SIZE+TOP_GUIDE_GAP`(40px), 좌측 눈금자의 top도 똑같이 40px로 내려서 눈금자와 이미지의 0~100%를 다시 정확히 일치시키면서, 눈금자 띠와 이미지 사이에 24px 빈 공간을 명확하게 확보(가로 패딩은 16px로 원복, 이번엔 위쪽만 요청받음) → CHANGELOG.md 기록. eslint/tsc 통과, **브라우저 실측 미실시** — 맨 위쪽 가이드 라벨이 이제 보이는지, 눈금자가 실제 가이드 위치와 정확히 맞는지 사용자가 직접 확인 필요. **후속(세션10에서 추가)**: 사용자가 "위쪽 공간은 생겼는데 좌측처럼 고정 실선 경계가 위에도 있어야 한다" 피드백 → 세션10 항목 참조
- 2026-06-22 세션10: 세션9 후속 — 캔버스 상단 경계선을 좌측 눈금자(`borderRight`)처럼 고정 실선으로 추가. `left-4 right-0 h-px bg-slate-700`을 `top: RULER_SIZE+TOP_GUIDE_GAP`(이미지/boardContainerRef 시작 위치)에 배치, 사용자가 만든 가이드(드래그 가능, 청록색)와 별개로 항상 표시되는 캔버스 경계 기준선 → CHANGELOG.md 기록. eslint/tsc 통과, **브라우저 실측 미실시** — 상단 경계선이 좌측 경계선과 같은 정렬로 보이는지 사용자가 직접 확인 필요. **후속(세션11에서 전부 원복)**: 사용자가 렌더된 HTML을 캡처해 보여주며 "이쪽은 원복, 내가 말한 건 HD일 때 위쪽 가이드라인이 쪼그라들어 실선처럼 보이는 거였다"고 정정 → 세션9~10의 패딩/경계선 변경 전부 되돌림, 진행 중인 작업 항목 참조
- 2026-06-22 세션11: 세션9~10에서 추가한 `TOP_GUIDE_GAP`/좌측눈금자 위치보정/캔버스 상단 경계선을 전부 원복(바깥패딩 16px, 좌측눈금자 `top-4` 원래대로). 세션8의 overflow-clip 수정(가이드 오버레이를 DroppableBoard 밖으로)과 imageRatio onLoad 갱신은 무관한 별개 수정이라 유지. eslint/tsc 통과 → CHANGELOG.md 기록.
  **후속(세션12에서 해결)**: 사용자가 스크린샷(1.png)으로 직접 보여줘 확정 — 세션12 항목 참조
- 2026-06-22 세션12: 위쪽 눈금자가 브라우저 줌(Ctrl+스크롤) 100%에서 거의 안 보이게 얇아지고 90%에서는 정상으로 보이던 문제 해결. 원인은 눈금자 래퍼의 `transform: scale(zoom)`을 zoom=1(기본값)에서도 항상 적용해 브라우저가 별도 컴포지팅 레이어를 만들었고, 그 안에서 16px 고정 높이 요소가 브라우저 줌 배율에 따라 서브픽셀로 어긋나 렌더링됐던 것. `zoom===1`일 때는 transform/transition을 style에서 빼서 컴포지팅 레이어 생성을 막음(앱 자체 확대/축소를 실제로 쓸 때만 transform 적용) → CHANGELOG.md 기록. eslint/tsc 통과, **브라우저 실측 미실시** — 브라우저 줌 100%에서 위쪽 눈금자가 정상 렌더되는지 사용자가 직접 확인 필요. **후속(세션13에서 추가 조치)**: transform 제거만으로는 부족 → 세션13 항목 참조
- 2026-06-22 세션13: 사용자가 개발자도구로 직접 눈금자 래퍼의 `padding-top: 16px`를 0으로 바꿔보고 정상적으로 보임을 확인 후 반영 요청 → `paddingTop`을 완전히 제거(`paddingLeft`는 유지). 위쪽 눈금자는 이제 이미지 상단과 겹쳐서(z-index가 더 높아 위에 그려짐) 표시되는 트레이드오프 감수, 좌측 눈금자는 기존처럼 paddingLeft 공간에 배치되어 안 겹침 → CHANGELOG.md 기록. eslint/tsc 통과. 진행 중인 작업 없음 — 최종 빌드 기준 브라우저 재확인만 필요.
- 2026-06-22 세션14: 위젯 잠금(전역 `canvasLocked`) 시 자유모드 위젯 본체 커서가 `cursor-not-allowed`(브라우저 차단/X 아이콘)였는데, 잠금은 드래그/리사이즈만 막고 클릭으로 선택해 우측 패널 스타일 설정은 그대로 바꿀 수 있어서 오해를 주는 커서였음 → `cursor-pointer`로 교체 → CHANGELOG.md 기록. eslint/tsc 통과. 진행 중인 작업 없음.
- 2026-06-22 세션15: TaskCreate 4건 — ①Ctrl+C/V 붙여넣기 위치를 마우스 좌표 기준으로(`lastCanvasMousePosRef`, 그룹 상대배치 유지) ②좌측 Redis 해시키 트리에 검색 추가(키 경로 + 필드명 `SUM_CONN_CNT` 등, 첫 포커스 시 모든 leaf 키 필드명 백그라운드 색인 후 `filterRedisTree`로 재귀 필터, fuzzy 매칭) — 사용자가 "필드값 전체를 부팅 시 긁어오면 어떻냐"고 제안 → 값(HGETALL) 전체 캐싱은 비추천(실시간이라 즉시 stale, 키 많으면 부팅 지연), "필드 이름만(HKEYS)"을 기존 키 SCAN 시점에 캐싱하는 게 좋겠다고 답변(미구현, BE 변경 필요) ③위젯 잠금 시 선택 위젯 강조(amber 링+글로우, 호버 액션 사라지는 잠금 상태에서 시인성 확보) ④하이라이트 모션이 텍스트 div가 아니라 위젯 박스 전체(absolute inset-0 오버레이)를 덮도록 수정해 valueOffsetX/Y 세밀조정과 무관하게 항상 박스 전체에 배경색 채워지게 함(TaskCreate/TaskView/RollingDisplay 3곳 동일 적용) → CHANGELOG.md 기록. eslint/tsc 통과, **브라우저 실측 미실시** — 4가지 모두 사용자가 직접 확인 필요. **후속(세션16에서 검색 버그 수정)**: 사용자가 "SUM_CONN_CNT로 검색해도 안 나온다"고 재현 → 세션16 항목 참조
- 2026-06-22 세션16: 세션15 검색 색인이 `SUM_CONN_CNT`를 못 찾던 버그 수정. 원인은 "해시그룹"(`IC:CTIQ:0` 등)의 진짜 컬럼명이 해시 필드명이 아니라 필드값(JSON) 안에 있는데, 색인이 `Object.keys(hashEntries)`(필드명=큐/그룹 ID)만 보고 있었던 것 — `RedisHashFieldItems`의 JSON 파싱 로직을 `getRedisHashColumns()`로 뽑아 검색 색인에도 동일 적용 → CHANGELOG.md 기록. eslint/tsc 통과. 진행 중인 작업 없음 — 브라우저에서 SUM_CONN_CNT 검색 시 IC:CTIQ:0 등이 바로 나오는지 확인 필요. **후속(세션17에서 BE로 이전)**: 사용자가 "이 검색 기능 때문에 웹 전체가 느려졌다"고 보고 → 클라이언트 병렬 색인 방식이 원인, 서버 캐시 방식으로 교체 → 세션17 항목 참조
- 2026-06-22 세션17: 검색 색인 방식을 클라이언트 병렬조회(`Promise.all`)에서 서버 캐시로 전환(BE `BT-ADMIN-SERVICE-TASKBOARD` 연동, 상세는 그쪽 CHANGELOG.md). FE는 `ensureFieldIndex` 등 클라이언트 색인 로직 전부 제거, 신규 `useGetRedisHashColumns()` 훅으로 서버 캐시(`GET /api/taskboard/redis/hash-columns`, BFF flow `taskboard-redis-hashcolumns`/V92 마이그레이션)를 그대로 받아 `filterRedisTree`에 전달. `useRefreshRedisHashKeys`가 새로고침 시 컬럼 캐시도 같이 invalidate → CHANGELOG.md 기록. eslint/tsc 통과, BE compileJava 성공. **브라우저 실측 미실시** — DB에 V92 마이그레이션 적용 후 검색이 즉시 동작하는지 확인 필요. 진행 중인 작업 없음.
