# TASKBOARD 작업 프롬프트 로그

> 사용자가 입력한 프롬프트만 날짜/시간 순으로 기록합니다.
> Claude 응답은 포함하지 않습니다.

---

## 2026-06-30

### [11:xx] 세션 재시작 (블루스크린 후)

```
아오.... 블루스크린떠서 컴퓨터 재부팅됐어... 이어서 해줘 ..
```

---

### [12:xx] 기능 요청 및 의견

```
1. task-create에서 왼쪽 위젯에
 # Redis 해시 키에서 -> Redis 명칭변경
 # 위젯항목에서 API, DB Query는  외부항목으로 분리해줘
2. 지금 Flow를 보면 전광판내에서 조회항목(큐/그룹/상담사) 이부분을 선택하여 해당 전광판의 전체에 대해서 조회를하는데, 예를들어 
 Rolling 할때, 같은 페이지를 A센터, B센터, C센터로 분리되어 Rolling이 아주 잘되고있는데, 
 추가적으로 한페이지내에서 그룹을 3rows로 나누어, A센터, B센터, C센터를 보게할려면 어떤방식으로 진행해야되지?
 
 추가적으로 위젯테이블끼리 join을 걸려면 어떤방식으로 해야돼?
 예를들어 상담그룹 데이터가 있고, 상담그룹별 이석사유코드는 서로 다른 위젯테이블인데, 이부분을 하나의 테이블로 합쳐져서 보이게 할수도있어?
  #  소스를 수정하지는 말고 의견만 부탁할께
  
  그리고, 전광판 실행할때 그냥 실행할때 ▶ 이버튼으로 실행하는데, 옆에 버튼을 하나더 추가해서 새창으로 띄우기 기능을 만들어줘
  그리고, 전광판 BFF통해서 외부키 연동으로 login없이 전광판을 실행하고싶은데, 소스내 TASKBOARD수정만을 이용해서 기능개발해줘 
   # taskboard-api / JVKksRE5AafZROD_LWxRIrnGB9894Ula 이게 전광판 외부키야
   # 설정 > 시스템 > 플랫폼 > 외부 앱 연동에 외부키 연결해서 세션없이 연동하는 부분이 있어 
   
  방법 A — 위젯별 고정 그룹 ID 필드 (권장)
  방법 B — 레이아웃 구역(Zone) 개념 도입
  방법 C — 현재 방식 유지 + Rolling 페이지 3개 활용
  
  >> A 방법이 좋아보이긴 할거같은데... 흠 어렵네 구역을 주기가.
  
질문 3 — 위젯 테이블 간 JOIN
  >> 단순 한개의 Join이 아니라, 어떤 방식의 쪼인이 들어올수있을지 몰라서... 음 위젯들을 서로 만들고 Key연결을 만들어서 행끼리 같은 값을 Mapping 시켜서 같은값끼리 연결시키는 구조는 어때??
  
전광판 롤링 시작버튼에서도 새창으로 열기 기능이 있으면 좋겠어 
여기서 수정된것도 있고. 수정하고있는 부분도 있어.. 확인부탁할께
```

---

### [이후] 계속 작업

```
쭉 이어서 해줘
앞으로 내가 쓴 prompt를 별도로 md파일에 미리 저장할까... 딱 내가 쓴말만...
```

---
