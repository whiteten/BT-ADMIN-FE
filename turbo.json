{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "stream",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**"],
      "env": ["NX_PUBLIC_REACT_QUERY_DEVTOOLS"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "passThroughEnv": ["SERVE_OPEN", "SERVE_NO_OPEN", "MF_REMOTE_HOST", "NX_PUBLIC_REACT_QUERY_DEVTOOLS", "NODE_OPTIONS"]
    }
  }
}
